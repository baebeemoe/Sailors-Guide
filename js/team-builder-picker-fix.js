// Team Builder picker v5: rarity-grouped partners, Adventurers and owned Monsters.
function pickerRarityRank(x){return ({UR:0,SSR:1,SR:2,R:3,N:4})[x.rarity]??99}
function pickerSorted(choices){return [...choices].sort((a,b)=>pickerRarityRank(a)-pickerRarityRank(b)||(allOwnable.indexOf(a)-allOwnable.indexOf(b)))}
function pickerRoleRows(x){const r=rank(x);return Object.keys(ROLE_NAMES).filter(k=>r[k]>=3).sort((a,b)=>r[b]-r[a]).slice(0,3).map((k,i)=>`<div class="picker-role-row ${i===0?'primary':''}"><span>${ROLE_NAMES[k]}</span><strong>${roleTier(r[k])}</strong></div>`).join('')}
function pickerCard(x){const r=rank(x);const art=x.image?`<div class="picker-icon picker-thumb"><img src="./${x.image}" alt="${x.name}" loading="lazy" onerror="this.parentElement.classList.add('image-failed');this.remove()"><span class="picker-fallback">${x.icon||'✦'}</span></div>`:`<div class="picker-icon picker-thumb image-failed"><span class="picker-fallback">${x.icon||'✦'}</span></div>`;const badge=x.type==='adventurer'?`<span class="picker-tier">TIER ${x.tier||''}</span>`:`<span class="picker-rarity rarity-${String(x.rarity).toLowerCase()}">${x.rarity}</span>`;return `<button class="picker-card picker-card-v2 ${x.type==='adventurer'?'picker-adventurer-card':''}" data-id="${x.id}">${art}<div class="picker-copy"><div class="picker-name-line"><strong>${x.name}</strong>${badge}</div><small>${x.element} · ${x.role}${r.verified?'':' · Provisional'}</small><div class="picker-role-list">${pickerRoleRows(x)||`<div class="picker-role-row primary"><span>${x.role}</span><strong>—</strong></div>`}</div></div></button>`}
function monsterPickerCard(x){return `<button class="picker-card picker-card-v2 monster-picker-card" data-id="${x.id}"><div class="picker-icon picker-thumb image-failed"><span class="picker-fallback">◈</span></div><div class="picker-copy"><div class="picker-name-line"><strong>${x.name}</strong><span class="picker-rarity rarity-${x.rarity.toLowerCase()}">${x.rarity}</span></div><small>${x.element!=='Unknown'?x.element+' · ':''}Monster${x.verified?' · Skill Verified':''}</small><div class="picker-role-list"><div class="picker-role-row primary"><span>Status</span><strong>${x.verified?'Verified':'Owned'}</strong></div></div></div></button>`}
openPicker=function(slot){activeSlot=slot;pickerTitle.textContent=slotTitle(slot);const choices=pickerSorted(choicesForSlot(slot));if(!choices.length){pickerGrid.innerHTML=`<p>No owned ${slot==='monster'?'Monsters':'characters'} available for this slot. Add them in Step 1.</p>`}else if(slot==='monster'){pickerGrid.innerHTML=['UR','SSR','SR','R'].map(rarity=>{const units=choices.filter(x=>x.rarity===rarity);return units.length?`<section class="picker-rarity-section"><div class="picker-rarity-heading rarity-${rarity.toLowerCase()}"><span>${rarity}</span><strong>${rarity} MONSTERS</strong></div><div class="picker-rarity-grid">${units.map(monsterPickerCard).join('')}</div></section>`:''}).join('')}else if(slot==='hero'){pickerGrid.innerHTML=`<div class="picker-adventurer-grid">${choices.map(pickerCard).join('')}</div>`}else{const groups=['SSR','SR'];pickerGrid.innerHTML=groups.map(rarity=>{const units=choices.filter(x=>x.rarity===rarity);return units.length?`<section class="picker-rarity-section"><div class="picker-rarity-heading rarity-${rarity.toLowerCase()}"><span>${rarity}</span><strong>${rarity} ${slot.startsWith('battle')?'BATTLE PARTNERS':'ASSIST PARTNERS'}</strong></div><div class="picker-rarity-grid">${units.map(pickerCard).join('')}</div></section>`:''}).join('')}
pickerGrid.querySelectorAll('.picker-card').forEach(card=>card.addEventListener('click',()=>{const selected=choices.find(x=>x.id===card.dataset.id);team[activeSlot]=selected;updateSlot(activeSlot,selected);if(activeSlot==='hero')adventurerSelect.value=selected.id;closeModal();updateAnalysis()}));backdrop.hidden=false;document.body.classList.add('modal-open')};